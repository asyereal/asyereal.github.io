#!/usr/bin/env bash

# NEVER miss version number update again
$EDITOR package.json

# Add changes to git.
git add -A

# Commit changes.
#msg="Did something `date`"
#if [ $# -eq 1 ]
#  then msg="$1"
#fi
git commit

# Push source and build repos.
git push 
